# Local development: public Stage feed permits localhost CORS.
# GitHub Pages uses the repository variable pointing to app.eigair.com instead.
# Never use authenticated /api/live-wind or put tokens in this value.
VITE_THERMALBASE_WEATHER_URL=https://stage.thermalbase.ch/api/public/weather/jungfrau?observations=4